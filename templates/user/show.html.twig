{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <div class="container"">
        <div class="row">
            <div class="table-responsive" id="mydatatable-container">
                <div class="table-responsive" id="mydatatable-container">
                    <table class="records_list table table-striped table-bordered table-hover w-100" id="mydatatable">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ user.telefono }}</td>
            </tr>

            <tr>
                <th>Idrol</th>
                <td>{{ user.idrol }}</td>
            </tr>
        </tbody>
        <tfoot>
        </tfoot>
    </table>
    </div>

    </div>
    </div>
    </div>

<div class="form-group">
    <a class="btn btn-secondary" href="{{ path('negocio_index') }}" role="button">Regresar</a>

    <a class="btn btn-warning" href="{{ path('user_edit', {'id': user.id}) }}" role="button">Editar</a>

        {{ include('user/_delete_form.html.twig') }}
</div>

{% endblock %}
